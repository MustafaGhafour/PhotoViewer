import { PhotoViewerOptions, PhotoViewer as PhotoViewerBase } from ".";
export * from './photoviewer.common';
export declare class PhotoViewer implements PhotoViewerBase {
    private static readonly CLOSE_PHOTO_REQUEST;
    private _currentResolve;
    constructor();
    showGallery(imagesArray: Array<string>, options?: PhotoViewerOptions): Promise<void>;
    private onActivityResult;
}
